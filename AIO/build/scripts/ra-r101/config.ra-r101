#
# Please modify KERNELPATH, KERNELARCH and TOOLPREFIX to meet your environment
#

export CUST_PROJ_PATH=<android path>
export KERNELPATH=<kernel path>
export KERNELARCH=<arm or arm64>
export TOOLCHAIN=<toolchain path>
export TOOLPREFIX=<toolchain prefix>
export CROSS_COMPILE=${TOOLCHAIN}/${TOOLPREFIX}
export KDIR=${KERNELPATH}
export ARCH=${KERNELARCH}

